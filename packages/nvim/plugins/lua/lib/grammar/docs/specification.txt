GRAMMAR
	OPERATOR=OPERATOR∙MODIFIER
	MODIFIER
		TYPES
			PRE
			POST
	OBJECT=MODIFIER∙OBJECT
	MOTION=
	TARGET=OBJECT|MOTION|JUMP
		TYPES
			CONTINUOUS
			DISCONTINUOUS
				g;|g,|g<space>
				MULTICURSORS
	OPERATION=COUNT∙OPERATOR∙COUNT∙TARGET
	FINAL=COUNT∙OPERATOR∙MODIFIER∙COUNT∙MODIFIER∙TARGET
REFERENCE
	OPERATORS
		MODIFIERS
		OPERATORS
			y|c|d
			g<spc>=NORMAL
			g;=GLOBAL
			g,=VGLOBAL
				g.=REPEAT()
			g/=SEARCH

			ys|ds|cs

			#gi=INSERT
			#ga=APPEND
			#g[=GOTO-START
			#g]=GOTO-END

			R=REPLACE
			gs=SUBSTITUTE
			gx=EXCHANGE|SWAP

			gy=DUPLICATE
			gl=ALIGN
			g==FORMAT
			gc=CASE
				gcl=LOWERCASE
				gcu=UPPERCASE
				gct=TITLECASE

				gcc=CAMEL
				gcp=PASCAL

				gc.=DOT
				gc-=HYPEN(KEBAB)
				gc_=SNAKE
				gc<spc>=SPACE
				gck=TITLE-HYPHEN

				gcs=SENTENCE
			gb=BROWSE

			gk=COMMENT
		HOOKS
			HIGHLIGHT
		META
			ARITY=BO|TO|NO
			ORDER=HOO
			GROUP=GO
				CORE=DELETE|YANK|CHANGE
			INTERACTIVITY=IO
	OBJECTS
		MODIFIERS
			g=𝛈𝛂(modifier-less)
			SELECTION
				i|a = inner|around
					i|I=
					a|A=
				s|d = SURROUND|DELIMINATOR
					s|S=
					d|D=
			HIGHER-ORDER
				[|] = START|END
				n|p = NEXT|PREVIOUS
				h|j|k|l = UP|DOWN|LEFT|RIGHT(TREE)
				b = BLOCK
				m = BULK|RANGE|MULTIPLE|SCOPE?
					gu r iw ip
					gu r ip iw
				r = REMOTE
				v=VISUAL
					CR = TO CONFIRM THE SELECTION
					h|j|k|l = FOR SELECTION BASED ON EXPANSION|CONTRACTION
						H|J|K|L = REVERSE DIRECTION[UNSELECTION]
					n|N|p|P = NEXT|PREVIOUS IN FORWARD|REVERSE DIRECTION
						n|p = NEXT|PREVIOUS[SELECTION]
						N|P = REVERSE DIRECTION[UNSELECTION]
					(|) = EXPANSION|CONTRACTION
						IN CASE OF NON-SURROUNDING TEXT DO BOTH n|p TO SELECT BOTH NEXT AND PREVIOUS OBJECT
					s|m|c = SINGLE|MULTIPLE|CONTINOUS-MODE
					t = CHANGE-TARGET
			NAMESPACES
				<space> = BUFFER (TS|LSP|FILETYPE)
				<comma> = CONTEXT(TS|LSP|FILETYPE)
			NOT-AVAILABLE
				h|j|k|l|w|b|e|ge|....=𝛈𝛂
		OBJECTS
			GENERAL
				gv=SELECTED
				??=YANKED
				??=PASTED
				??=INSERTED
				??=CHANGED
			TEXT
				CHARACTER
				WORD
				LINE
				SENTENCE
				PARAGRAPH
				i=INDENT
				e=ENTIRE
				ga|gb=AFTER|BEFORE
			TREESITTER
				KEYWORD
				IDENTIFIER
				LITERAL
				EXPRESSION
				STATEMENT
				BLOCK=(){}[]

				CONDITIONAL
				LOOP
				FUNCTION
					APPLICATION
					ARGUMENT
					PARAMETER
			VCS
				HUNK
		GROUPS
			iq = '"`
			ib = ([{
		HOOKS
		FEATURES
			SEEK=CLOSEST(LINE|LINES)|FORWARD|BACKWARD
			CONTEXT=TS|LSP|FILETYPE
				#FOR EX LSP OBJECTS BASED ON CURRENT CONTEXT LIKE ic CAN MEAN BOTH CONDITIONAL OR CLASS BASED ON CONTEXT
				#KIND OF LIKE MULTI-TEXT OBJECTS
		META
			ARITY=BO|NO
				BO=WORD
				NO=IF|ELIF|ELSE
	MOTIONS
		MODIFIERS
		MOTIONS
		META
			DIMENSION=2D|4D
				2D=NP
				4D=HJKL
			ARITY=2D|ND
				2D=START|END
				3D=IF|ELIF|ELSE
MESS
	COMPOSITION = OPERATOR|OBJECT
		COMPOSITION MODES LIKE AND|OR??
