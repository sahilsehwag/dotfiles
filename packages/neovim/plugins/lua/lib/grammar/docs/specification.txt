GRAMMAR
	OPERATOR=OPERATOR∙MODIFIER
	MODIFIER
		TYPES
			PRE
			POST
	OBJECT=MODIFIER∙OBJECT
	MOTION=
	TARGET=OBJECT|MOTION
		TYPES
			CONTINUOUS
			DISCONTINUOUS
				g;|g,|g<space>
				Multicursors
	OPERATION=COUNT∙OPERATOR∙COUNT∙TARGET
	FINAL=COUNT∙OPERATOR∙MODIFIER∙COUNT∙MODIFIER.TARGET
HOOKS=OPERATOR|OBJECT
	VISUALIZATION
REFERENCE
	OPERATORS
		MODIFIERS
		OPERATORS
			y|c|d
			g =normal
			g;=global
			g,=vglobal
			g.=repeat
			g/=search

			ys|ds|cs

			gi=insert
			ga=append
			g[=goto-start
			g]=goto-end

			gy=duplicate

			R=replace
			gs=substitute
			gx=swap[exchange]

			g==format
			gk=comment
			gc=caser
			gb=browse
	OBJECTS
		MODIFIERS
			TARGET
				i|a = inner|around
				s|d = SURROUND|DELIMINATOR
			HIGHER-ORDER
				[|] = START|END OF OBJECT
				n|p = NEXT|PREVIOUS OBJECT
				h|j|k|l = UP|DOWN|LEFT|RIGHT(TREE)
				v=VISUAL
					CR = TO CONFIRM THE SELECTION
					h|j|k|l = FOR SELECTION BASED ON EXPANSION|CONTRACTION
						H|J|K|L = REVERSE DIRECTION[UNSELECTION]
					n|N|p|P = NEXT|PREVIOUS IN FORWARD|REVERSE DIRECTION
						n|p = NEXT|PREVIOUS[SELECTION]
						N|P = REVERSE DIRECTION[UNSELECTION]
					(|) = EXPANSION|CONTRACTION
						IN CASE OF NON-SURROUNDING TEXT DO BOTH n|p TO SELECT BOTH NEXT AND PREVIOUS OBJECT
					s|m|c = SINGLE|MULTIPLE|CONTINOUS-MODE
					t = CHANGE-TARGET
				b = BLOCK
				r|m = BULK|RANGE|MULTIPLE|SCOPE?
					gu r iw ip
					gu r ip iw
			NAMESPACES
				space=LSP
				comma=BUFFER
				?????=CONTEXT
		OBJECTS
			ga|gb=AFTER|BEFORE
	MOTIONS
		MODIFIERS
		MOTIONS
MESS
	MULTI-TEXT OBJECTS
		LIKE MULTIPLE(MULTI) TEXT OBJECTS LIKE iq|ib
	CONTEXTUAL OBJECT
		FOR EX LSP OBJECTS BASED ON CURRENT CONTEXT LIKE ic CAN MEAN BOTH CONDITIONAL OR CLASS BASED ON CONTEXT
		KIND OF LIKE MULTI-TEXT OBJECTS
	REMOTE (easymotion)
