# which-key.conf — Key tree configuration
#
# Format: key_sequence | label | command
#
# - Empty command = namespace (group of keys)
# - Nested keys use space separation (e.g., "p c" is child of "p")
#
# Command prefixes:
#   !cmd     → run in bottom popup
#   %cmd     → split pane vertically (side-by-side)
#   =cmd     → split pane horizontally (top/bottom)
#   >cmd     → run in new window
#   &cmd     → run in new session (and switch to it)
#   (none)   → raw tmux command
#
# Auto-close behavior (for !, %, =, >, & prefixes):
#   Default  → auto-close on success (exit 0); retain on failure (shows exit code + waits)
#   !~cmd    → always retain (use for interactive commands or commands with important output)

# Project
p     | project
p e   | nvim                  | %nvim .
p E   | cursor                | !cursor .
p c   | lint + tc + test      | !ucheck
p s   | setup                 | !usetup
p i   | install               | !jz install
p f   | new-feature           | !ufeat
p n   | new-feature-from-main | >arh-feature
p F   | new-tree              | >arh-feature-tree
p t   | new-tree + pick       | >arh-feature-tree --pick
p T   | new-tree + publish    | >arh-feature-tree --pick --publish
p m   | merge-stack           | >arh-merge
p r   | rebase
p r r | rebase-till-current   | >arh rebase
p r t | rebase-tree           | >arh rebase -t .
p r m | restack-to-main       | >arh-restack-to-main
p p   | publish               | >arh publish
p d   | run                   | !urun
p a   | auth                  | !~ussh
p A   | agents-sidebar        | run-shell "~/.config/tmux/scripts/agents.sh"
#p a   | claude-in-wt       | >claude

# AI
a     | ai
a p   | pi                    | %pi
a P   | pi (new window)       | >pi
a o   | omp                   | %omp
a O   | omp (new window)      | >omp
a d   | opencode              | %opencode
a D   | opencode (new window) | >opencode
a c   | claude               | %claude
a C   | claude (new window)   | >claude

# Git
#g   | Git
#g s | Status  | !git status
#g p | Push    | !git push
#g l | Log     | !git log --oneline -20

# Sessions
#s | Sessions  | choose-tree -sZ -O name
#d | Detach    | detach
#x | Kill      | confirm-before -p "Kill session? (y/n)" kill-session
