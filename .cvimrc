"PREFERENCES
	set localconfig
	let configpath = "C:/Users/sehwa/OneDrive/Workspace/Projects/Scripts/Enviornment Setup Scripts/Dotfiles/.cvimrc"

	set smoothscroll
	set noautofocus
	set scalehints
	set linkanimations
	"set showtabindices

	let mapleader = ","
	let barposition = "bottom"
	let blacklists = [""]

	let scrollstep = 100
	let scrollduration = 200
	let searchlimit = 20
	let zoomfactor = 0.1
	let defaultengine = "google"
"MAPPINGS
	"CHROME/cVIM MAPPINGS (c)
		map ; :
		map <Leader>cr :chrome://restart<CR>
	"SESSION MANAGEMENT (s)
		map <Leader>sn :mksession<Space>
		map <Leader>so :session<Space>
		map <Leader>sd :delsession<Space>
	"TAB MAPPINGS (t)
		map <Leader>tl :buffer<Space>
	"WINDOW MAPPINGS (w)
	"OPTIONS (o)
		map <Leader>oi :set showtabindices!<CR>
"UNMAPPINGS
	unmap w
	unmap s
"COMMANDS
